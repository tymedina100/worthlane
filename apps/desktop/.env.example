# Server-only URL for the shared Worthlane API. Do not expose it through a
# NEXT_PUBLIC_ variable; desktop browser requests use same-origin route handlers.
WORTHLANE_API_URL=http://localhost:3001/api

// Expo provides process.env at runtime via Babel transforms.
// This declaration satisfies TypeScript in environments without @types/node.
declare const process: { env: Record<string, string | undefined> };
